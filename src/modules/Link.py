from pydantic import BaseModel, Field


class Link(BaseModel):
    """Link to a report generated by a worker"""
    link: str = Field(
        description="the URL to the link found by the agent.",
    )
    description: str = Field(
        description="Brief overview of the URL contents.",
    )
